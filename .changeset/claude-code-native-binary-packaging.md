---
---

No package release is required for this PR. It bundles the `@anthropic-ai/claude-agent-sdk` native CLI binary into production Electron builds (app `package.json`, `electron-builder.yml`, `before-pack.js`) and does not change any published workspace package.
