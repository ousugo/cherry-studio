// `bootConfigSchemas` is auto-generated (see its header) — keep this named
// re-export in sync when the generator adds a new export.
export { type BootConfigSchema, DefaultBootConfig } from './bootConfigSchemas'
export type { BootConfigKey, BootConfigPreferenceKeys } from './bootConfigTypes'
